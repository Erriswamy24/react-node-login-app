This is a simple login application built using React (Frontend) and Node.js with Express (Backend).

Features:
- Login page with username and password
- Backend API validation
- Error message for invalid credentials
- Welcome page after successful login
- Username stored using localStorage

Login Credentials:

Username: admin
Password: admin

Backend Setup:
cd backend
node server.js

Frontend Setup:
cd frontend
npm start
